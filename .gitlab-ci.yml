image: conda/miniconda3
stages:
  - test
  - code_quality
  - build-env
  - build-env-plugins
  - build-tool
  - doc
  - deploy
  - visualize
  - build
  - release

variables:
  GIT_SUBMODULE_STRATEGY: recursive

include:
    - 'conda_build_config.yaml'
    - 'conda_receipe/conda.gitlab-ci.yml'